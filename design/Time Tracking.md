I start working at 18:00, i dont want to track every tasks individually, but still want to see the progression of the day

Information at the top: 
Started at 18:00

if the user updates tasks, it could say :updates at 18:20

but maybe we want to keep the started information somewhere?

---
ok we need to polish this just a bit further, i thought about my day to day use: what i really need is a 'started at' time, this should write itself in the local storage when i open the program for the day, which means this should reset at midnight. this is also a bit tricky, because i know some people work in the night, so maybe a hard reset is not the best solution in the long run, rather another user setting of 'day resets at x' later this can be helped with the 'end of day journal' which could reset the start time and set it new once the user starts the program or in the midlong run: when the user finished planning the day, the 'started' time should reflect the real start of the day. for now this is a soft value with a fixed time reset because i dont know what the best solution is so i guess this would need to be saved to the local storage the estimated finished time calculates itself per task by this 'started' time where task 1 starts directly at the 'started' time and the estimate is calculated by the started + task time the task 2 will calc its estimated finished from the previous task and so on why this is important: i dont want a running 'now' timestamp, because some people like time tracking, others dont. so if the user checks the first task, before i have 'real' time tracking' it could set a timestamp for the 'finished time' and calc the 'real time' by the difference from the timestamp and the 'time start' the next task will then calculate the 'estimated finished' by the last timestamp, and so on there should be a 'restart starting time' option (later) that is important for when the user had an unplanned break or distraction, so the whole plan for the day is not 'wrong' and the next OPEN tasks should then calculate new from this 'updated' time, and then we could change the label to 'restarted at' but small steps for the whole thing, this is just the context you need